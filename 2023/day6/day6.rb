#!/usr/bin/env ruby

puts "We're looking for the domain of the inequality `hold(time - hold) >= distance`, where `hold` is the time you hold the button, and `distance` is the distance you need to beat. I just used Wolfram Alpha to solve this :)"
